version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PORT=8000
    # En desarrollo, usamos uvicorn con reload
    command: uvicorn src.server:app --host 0.0.0.0 --port 8000 --reload
